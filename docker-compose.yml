version: "3"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    env_file: .env
    depends_on:
      - db
    volumes:
      - ./db:/opt/db
  db:
    image: postgres:13
    ports:
      - 5432:5432
    env_file: .env
